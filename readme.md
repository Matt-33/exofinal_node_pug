-   Pour lancer le projet creer un fichier .env et y mettre:
    APP_ENV=development
    APP_HOST=127.0.0.1
    APP_PORT=3000

-Installer les dépendances :
npm install pug dotenv dayjs

et lancer le projet :
node server.js
